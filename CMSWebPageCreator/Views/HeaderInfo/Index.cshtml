@model IEnumerable<CMSWebPageCreator.Models.HeaderInfo>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PageCreateParentId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.HeaderContent)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ContentType)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PageCreateParentId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.HeaderContent)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ContentType)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.HeaderItem">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.HeaderItem">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.HeaderItem">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
