@model IEnumerable<CMSWebPageCreator.Models.BodyInfo>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PageCreateParentId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BodyContent)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ContentType)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PageCreateParentId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BodyContent)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ContentType)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.BodyItem">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.BodyItem">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.BodyItem">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
